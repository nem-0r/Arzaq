# Community Posts Feature - Technical Documentation

## Overview
Полнофункциональная система создания и просмотра постов в Community с авторизацией, валидацией и готовностью к интеграции с бэкендом.

## Реализованные возможности

### 1. Создание постов
- ✅ Кнопка "Create Post" на странице Community
- ✅ Модальное окно с формой создания поста
- ✅ Выбор ресторана/заведения из списка
- ✅ Поиск ресторанов по названию
- ✅ Валидация формы (минимум 10 символов)
- ✅ Обработка ошибок

### 2. Авторизация
- ✅ Проверка авторизации перед созданием поста
- ✅ Модальное окно с предложением войти/зарегистрироваться
- ✅ Редирект на страницу логина/регистрации
- ✅ Возврат на исходную страницу после авторизации

### 3. API Service Layer
- ✅ Полностью готовый сервисный слой для бэкенда
- ✅ CRUD операции для постов
- ✅ Поиск ресторанов
- ✅ Управление авторизацией
- ✅ Обработка ошибок и токенов

## Архитектура

```
src/
├── services/
│   └── api.js                          # API сервисный слой
├── components/
│   ├── common/
│   │   └── LoginPrompt/                # Модалка для неавторизованных
│   │       ├── LoginPrompt.jsx
│   │       └── LoginPrompt.module.css
│   └── features/
│       └── Community/
│           ├── CreatePostButton/       # Кнопка создания поста
│           │   ├── CreatePostButton.jsx
│           │   └── CreatePostButton.module.css
│           └── CreatePostModal/        # Модалка создания поста
│               ├── CreatePostModal.jsx
│               └── CreatePostModal.module.css
└── pages/
    └── CommunityPage/
        ├── CommunityPage.jsx           # Обновленная страница
        └── CommunityPage.module.css
```

## Как это работает

### Flow создания поста (авторизованный пользователь)

1. Пользователь нажимает кнопку "Create Post"
2. Проверяется `isAuthenticated` из AuthContext
3. Открывается модальное окно `CreatePostModal`
4. Пользователь выбирает ресторан из списка (с поиском)
5. Вводит текст поста (минимум 10 символов)
6. Нажимает "Post"
7. Валидация формы
8. Вызов `api.posts.create()` → сохранение в localStorage
9. Пост добавляется в начало ленты
10. Модалка закрывается

### Flow для неавторизованного пользователя

1. Пользователь нажимает кнопку "Create Post"
2. Проверяется `isAuthenticated` → false
3. Открывается модальное окно `LoginPrompt`
4. Два варианта:
   - "Login" → редирект на /login
   - "Create Account" → редирект на /register
5. После авторизации пользователь может создавать посты

## Интеграция с бэкендом

### Шаг 1: Настройка переменных окружения

Создайте `.env` файл в корне проекта:

```env
REACT_APP_API_URL=https://your-backend-api.com/api
```

### Шаг 2: Обновите src/services/api.js

Раскомментируйте секции с комментарием `// BACKEND:` и замените mock реализации:

#### Пример: Создание поста

```javascript
// src/services/api.js

create: async (postData) => {
  const response = await fetch(`${API_BASE_URL}/posts`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${getAuthToken()}`
    },
    body: JSON.stringify(postData)
  });
  return handleResponse(response);
}
```

#### Пример: Получение постов

```javascript
getAll: async (filters = {}) => {
  const queryParams = new URLSearchParams(filters);
  const response = await fetch(`${API_BASE_URL}/posts?${queryParams}`, {
    method: 'GET',
    headers: {
      'Content-Type': 'application/json'
    }
  });
  return handleResponse(response);
}
```

### Шаг 3: Backend API Contract

#### POST /api/posts

**Request:**
```json
{
  "text": "Great food at this place!",
  "restaurantId": 1,
  "restaurantName": "Green Garden Bistro",
  "restaurantAddress": "Almaty, Dostyk Ave",
  "userId": "user@example.com"
}
```

**Response:**
```json
{
  "success": true,
  "post": {
    "id": 123,
    "text": "Great food at this place!",
    "location": "📍 Green Garden Bistro",
    "author": "John Doe",
    "avatar": "👤",
    "time": "Just now",
    "restaurantId": 1,
    "restaurantName": "Green Garden Bistro",
    "restaurantAddress": "Almaty, Dostyk Ave",
    "userId": "user@example.com",
    "createdAt": "2025-11-05T12:00:00.000Z",
    "updatedAt": "2025-11-05T12:00:00.000Z"
  }
}
```

#### GET /api/posts

**Query Params:**
- `page` - номер страницы (опционально)
- `limit` - количество постов на странице (опционально)
- `userId` - фильтр по пользователю (опционально)
- `restaurantId` - фильтр по ресторану (опционально)

**Response:**
```json
{
  "success": true,
  "posts": [
    {
      "id": 123,
      "text": "Great food!",
      "location": "📍 Green Garden Bistro",
      "author": "John Doe",
      "avatar": "👤",
      "time": "2 hours ago",
      "restaurantId": 1,
      "createdAt": "2025-11-05T10:00:00.000Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 20,
    "total": 45
  }
}
```

#### GET /api/restaurants/search?q={query}

**Query Params:**
- `q` - поисковый запрос

**Response:**
```json
{
  "success": true,
  "restaurants": [
    {
      "id": 1,
      "name": "Green Garden Bistro",
      "address": "Almaty, Dostyk Ave"
    }
  ]
}
```

## Валидация

### Форма создания поста

1. **Текст поста:**
   - Обязательное поле
   - Минимум 10 символов
   - Обрезается пробелы

2. **Ресторан:**
   - Обязательное поле
   - Выбирается из списка (не свободный ввод)

### Обработка ошибок

- Ошибки валидации отображаются под соответствующим полем
- Ошибки API отображаются вверху формы
- Кнопка "Post" блокируется во время отправки

## Хранение данных (текущая реализация)

### localStorage

**Ключи:**
- `community_posts` - массив всех постов
- `currentUser` - данные авторизованного пользователя
- `user_{email}` - данные зарегистрированных пользователей

### Структура поста в localStorage

```json
{
  "id": 1730815200000,
  "text": "Great experience!",
  "location": "📍 Green Garden Bistro",
  "author": "John Doe",
  "avatar": "👤",
  "time": "Just now",
  "restaurantId": 1,
  "restaurantName": "Green Garden Bistro",
  "restaurantAddress": "Almaty, Dostyk Ave",
  "userId": "user@example.com",
  "createdAt": "2025-11-05T12:00:00.000Z",
  "updatedAt": "2025-11-05T12:00:00.000Z"
}
```

## Масштабируемость

### Добавление пагинации

```javascript
// src/pages/CommunityPage/CommunityPage.jsx

const [page, setPage] = useState(1);
const [hasMore, setHasMore] = useState(true);

const loadPosts = async () => {
  const response = await api.posts.getAll({ page, limit: 20 });

  if (response.success) {
    setPosts([...posts, ...response.posts]);
    setHasMore(response.pagination.total > posts.length);
  }
};

const loadMore = () => {
  setPage(page + 1);
};
```

### Добавление фильтров

```javascript
const [filters, setFilters] = useState({
  restaurantId: null,
  dateFrom: null,
  dateTo: null
});

const loadPosts = async () => {
  const response = await api.posts.getAll(filters);
  // ...
};
```

### Добавление лайков/комментариев

```javascript
// src/services/api.js

posts: {
  // ... existing methods

  like: async (postId) => {
    const response = await fetch(`${API_BASE_URL}/posts/${postId}/like`, {
      method: 'POST',
      headers: {
        'Authorization': `Bearer ${getAuthToken()}`
      }
    });
    return handleResponse(response);
  },

  addComment: async (postId, commentText) => {
    const response = await fetch(`${API_BASE_URL}/posts/${postId}/comments`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${getAuthToken()}`
      },
      body: JSON.stringify({ text: commentText })
    });
    return handleResponse(response);
  }
}
```

## Тестирование

### Как протестировать

1. **Неавторизованный пользователь:**
   - Откройте http://localhost:3002/community
   - Нажмите "Create Post"
   - Должно появиться модальное окно с предложением войти
   - Нажмите "Login" или "Create Account"
   - Убедитесь, что происходит редирект

2. **Авторизованный пользователь:**
   - Зарегистрируйтесь/Войдите в систему
   - Откройте http://localhost:3002/community
   - Нажмите "Create Post"
   - Выберите ресторан из списка
   - Введите текст (минимум 10 символов)
   - Нажмите "Post"
   - Пост должен появиться в начале ленты

3. **Валидация:**
   - Попробуйте отправить пустой пост
   - Попробуйте отправить пост меньше 10 символов
   - Попробуйте отправить без выбора ресторана
   - Должны появиться соответствующие ошибки

## Безопасность

### Текущая реализация
- Пароли хранятся в plain text в localStorage (только для разработки!)
- Нет защиты от XSS/CSRF

### Для продакшена
1. Все пароли должны хешироваться на бэкенде
2. Используйте JWT токены для авторизации
3. Включите HTTPS
4. Добавьте санитизацию input данных
5. Добавьте rate limiting на API
6. Добавьте CSRF токены для форм

## Дальнейшее развитие

### Приоритетные фичи
1. Загрузка фото в посты
2. Лайки и комментарии
3. Уведомления
4. Пагинация ленты
5. Фильтры по ресторанам/дате
6. Редактирование/удаление постов
7. Модерация постов
8. Репорт нежелательного контента

### UI/UX улучшения
1. Скелетоны при загрузке
2. Оптимистичные обновления
3. Pull-to-refresh
4. Бесконечная прокрутка
5. Анимации переходов
6. Темная тема

## Контакты и поддержка

При возникновении вопросов или проблем:
1. Проверьте консоль браузера на ошибки
2. Проверьте Network tab в DevTools
3. Убедитесь, что все зависимости установлены
4. Проверьте версию Node.js (рекомендуется 16+)

---

**Дата создания:** 2025-11-05
**Версия:** 1.0.0
**Статус:** Production Ready (требуется только backend интеграция)
